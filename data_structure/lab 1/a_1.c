#include <stdio.h>
void main() {
    int radius;
    
   printf("enter the radius of the circle in cm:");
   scanf("%d",&radius);
   printf("the radius of the circle is :%f",radius*radius*3.14);
}